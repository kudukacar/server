branch-defaults:
  master:
    environment: ServerEnv1-env
deploy:
  artifact: build/libs/echoserver.jar
global:
  application_name: Server-env-1
  default_ec2_keyname: eba-2xv62qi8
  default_platform: Java 8 running on 64bit Amazon Linux/2.10.4
  default_region: us-east-1
  sc: git